server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Allow large request bodies for receipt image uploads (OCR)
    client_max_body_size 10M;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
