# ─────────────────────────────────────────────────────────────────────────────
# DO NOT commit real values to Git.
# This is a template. On the VM, create the real secret with:
#
#   kubectl create secret generic backend-secret \
#     --namespace spend-sense \
#     --from-literal=PORT=5000 \
#     --from-literal=mongoDBURL="mongodb://mongodb:27017/spendSense" \
#     --from-literal=JWT_SECRET="<your-secret>" \
#     --from-literal=SESSION_SECRET="<your-secret>" \
#     --from-literal=FRONTEND_URL="https://spendsense.duckdns.org" \
#     --from-literal=BACKEND_URL="https://spendsense.duckdns.org" \
#     --from-literal=GOOGLE_CLIENT_ID="<your-id>" \
#     --from-literal=GOOGLE_CLIENT_SECRET="<your-secret>" \
#     --from-literal=GEMINI_API_KEY="<your-key>" \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: spend-sense
type: Opaque
# Values below are base64-encoded placeholders — replace before applying.
# echo -n 'your-value' | base64
stringData:
  PORT: "5000"
  mongoDBURL: "mongodb://mongodb:27017/spendSense"
  JWT_SECRET: "CHANGE_ME"
  SESSION_SECRET: "CHANGE_ME"
  FRONTEND_URL: "https://spendsense.duckdns.org"
  BACKEND_URL: "https://spendsense.duckdns.org"
  GOOGLE_CLIENT_ID: "CHANGE_ME"
  GOOGLE_CLIENT_SECRET: "CHANGE_ME"
  GEMINI_API_KEY: "CHANGE_ME"
