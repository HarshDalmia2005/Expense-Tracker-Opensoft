apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: spend-sense
  labels:
    app: mongodb
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
  # ClusterIP — MongoDB is only reachable inside the cluster
  type: ClusterIP
